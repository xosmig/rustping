#![allow(dead_code)]

extern crate libc;
extern crate num;

mod icmp;
mod sys_return;
mod into_raw;

fn main() {

//    for i in (0..data.len() - 1).step_by(2) {
//        res = sum(res, (data[2 * i + 1] << 8) as u16 | data[2 * i] as u16);
//    }
    println!("Hello, world!");
}
